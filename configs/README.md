# Configurations

此目錄將保存可版本控制、與機器無關的 dataset、model 與 runtime 設定。

- 不放密碼、token、個人絕對路徑。
- 不放 model checkpoint 或大型特徵檔。
- 本機差異使用環境變數或未追蹤的 local override。

Dataset media profiles 位於 `datasets/media_profiles.toml`。這些設定描述可重建的衍生檔，不代表授權核准，也不允許覆寫原始影片。
